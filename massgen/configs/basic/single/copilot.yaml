# MassGen Copilot Configuration
# `uv run massgen --config @examples/basic/single/copilot.yaml "What is GitHub copilot?"`

agents:
  - id: "copilot-assistant"
    backend:
      type: "copilot"
      model: "gpt-5-mini"

ui:
  display_type: "textual_terminal"
  logging_enabled: true
